/**
 * Layer Sprite Naming:
 *     `${BodyPart}${LeftRight}${Glow?}${Pose}`
 * 
 * Use folder to distinguish which item the links are attached to.
 */


export {InheritColor, AddGlow as WithGlow, MakeLeftRight as WithLeftRight} from './Common'
export * as Arm from './Arm'